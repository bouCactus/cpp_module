#include "Fixed.hpp"
#include "Point.hpp"



bool bsp(Point const a, Point const b, Point const  c, Point const point)
{
  (void)a;
  (void)b;
  (void)c;
  (void)point;
  std::cout << "this function does not implmented yet" << std::endl;
  return (true);
}
